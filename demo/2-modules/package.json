{
  "name": "2-modules",
  "version": "1.0.0",
  "description": "1. De manière classique (par défaut) : `import * from node:fs` ou `const fs = require('node:fs')`\r - Exécution des fonctions ***callback***, une fois que l'événement ou le résultat est disponible\r 2. De manière asynchrone avec les promesses, il faut rajouter le dossier ***/promises*** à la suite du nom du module : `import * from node:fs/promises` `const fs = require('node:fs/promises')`\r - Exécution des fonctions `then()` en cas de succès et `catch()` en cas d'erreur\r 3. De manière synchrone, les fonctions sont appelées avec le suffixe ***Synch()*** : `import { readFileSync } from node:fs` `const { readFileSynch } = require('node:fs')`\r - Exécution bloquante",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
